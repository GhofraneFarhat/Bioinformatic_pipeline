  ## Input Data
input:
  path_to_input_gfa: #path to gfa file input
  path_to_input_fasta: #path to fasta file input

outdir_pipeline: 
prefix: plasgraph_plasbin_flow

classification:
  name: plASgraph2
  version: 2.0.0
  min_length:
  input_format: gfa
  parameters:
  classification_csv_result: #if you want to use a personnal classifier


  ## Output
  output:
    outdir_classification: #path to the folder of the classification tool output

binning:
  name: plasbin_flow
  version: 1.0.0

  input_format: gfa
  parameters: 

  plasbin_out_dir: 
  sample_name: #sample name parameter
  plasbin_tmp_dir: 
  gc_interval_file: 
  log_file: #log file for plasbin_flow tool parameter
  seed_len:
  seed_score: 
  assembler: 



  ## Output
  output:
    outdir_binning:  #path to the folder of the binning tool output
